import React from "react";
import Typography from "@mui/material/Typography";

const ArtistsPage = React.memo(() => {
  return (
    <div>
      <Typography variant="h3">Artists</Typography>
    </div>
  );
});

export default ArtistsPage;
